{
  "name": "limitelimite",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "debug": "DEBUG=socket.io:* nodemon --legacy-watch --nolazy --inspect=0.0.0.0:9229 app.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "finalhandler": "^1.1.2",
    "puppeteer": "^3.0.0",
    "serve-static": "^1.14.1",
    "socket.io": "^2.3.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.3"
  }
}
